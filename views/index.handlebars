
<body class="page-background" id="main-page">
    <div class="container">
<h1>Newscraper</h1>
        <div class="row">
            <div class="col-md-12">

                {{#each articles}}
                {{log this}}
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{this.title}}</h5>
                        <a href={{this.link}} class="card-link">
                            <i class="newspaper"></i> Read Article in New York Times</a>
                        <br>
                        <br>
                        <p class="card-text">Article Summary: {{this.summary}}</p>

                        <br>
                        <button type="button" class="btn btn-primary save" data-id={{this._id}}>Save Article</button>
                    </div>
                </div>
                {{/each}}

            </div>
        </div>
    </div>